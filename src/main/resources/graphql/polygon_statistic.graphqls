type PolygonStatistic {
    axis: [Axis],
    meta: Meta,
    initAxis: InitAxis,
    overlays: [Overlay],
    indicators: [Indicator],
    correlationRates: [PolygonCorrelationRate],
    colors: Color
}